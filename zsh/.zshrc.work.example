# ==============================================================================
#                            WORK MACHINE OVERRIDES
# ==============================================================================
# Copy this file to ~/.zshrc.work and customize for your work machine.
# This file is sourced by .zshrc but is NOT tracked in the dotfiles repo.

# pnpm
# export PNPM_HOME="/Users/mchen/Library/pnpm"
# case ":$PATH:" in
#   *":$PNPM_HOME:"*) ;;
#   *) export PATH="$PNPM_HOME:$PATH" ;;
# esac

# Work-specific aliases
# alias gowork="cd ~/Projects/work"
# alias gonaisa="cd ~/Projects/naisa"

# Naisa UI — lazy-load GitHub Packages token on first use
# _load_github_token() {
#   if [[ -z "$GITHUB_PACKAGES_TOKEN" ]] && command -v gh >/dev/null 2>&1; then
#     export GITHUB_PACKAGES_TOKEN="$(gh auth token 2>/dev/null)"
#   fi
# }
# precmd_functions+=(_load_github_token)
